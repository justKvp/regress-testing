---
project:
  # базовый URL
  baseUrl: "http://localhost"
  # базовый URL2
  baseUrl2: "http://localhost"
  allureTMSUrl: "http://localhost/tms/testCase/"
  datasource: "local.cfg.xml"

selenide:
  browser: "chrome"
  browserSize: "1920x1080"
  locale: "ru-RU"
  timeout: 10000
  #browserBinary: "" # не обязателен
  #driverBinary: ""  # не обязателен
  #reportsFolder: "" # не обязателен
  screenshots: false
  savePageSource: false

# не обязателен
selenoid:
  # не обязателен
  remote: "http://localhost:4444/wd/hub"